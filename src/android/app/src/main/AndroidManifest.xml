<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false"/>

    <uses-feature android:glEsVersion="0x00030002" android:required="true" />

    <uses-feature android:name="android.hardware.opengles.aep" android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 13 (API 33) 起:发通知必须运行时申请这个权限,不申请发不出去,
         高版本 (Android 13/14/15) 上 createNotificationChannel 之类的能跑,
         但 tryDismissRunningNotification 之类的会被静默拒掉。-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:name="org.citra.citra_emu.CitraApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:isGame="true"
        android:banner="@mipmap/ic_launcher"
        tools:targetApi="33">

        <activity
            android:name="org.citra.citra_emu.ui.main.MainActivity"
            android:theme="@style/CitraBase"
            android:resizeableActivity="false"
            android:exported="true">

            <!-- This intentfilter marks this Activity as the one that gets launched from Home screen. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="org.citra.citra_emu.features.settings.ui.SettingsActivity"
            android:configChanges="orientation|screenSize|uiMode"
            android:theme="@style/CitraSettingsBase"
            android:label="@string/preferences_settings"
            android:exported="false" />

        <activity
            android:name="org.citra.citra_emu.activities.EmulationActivity"
            android:resizeableActivity="false"
            android:theme="@style/CitraEmulationBase"
            android:launchMode="singleTop"
            android:exported="false" />

        <!-- Android 8 (API 26) 起 foreground service 必须显式声明 foregroundServiceType,
             14 (API 34) 起强制。这里是个常驻 emulator 通知服务,选 dataSync。-->
        <service
            android:name="org.citra.citra_emu.utils.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name="org.citra.citra_emu.activities.CustomFilePickerActivity"
            android:label="@string/app_name"
            android:theme="@style/FilePickerTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Android 12 (API 31) 起:所有 Service/Receiver/Provider 都必须显式 export,
             没声明 manifest 合并时一般会警告,运行时某些场景会拒绝启动。-->
        <service
            android:name="org.citra.citra_emu.utils.DirectoryInitialization"
            android:exported="false" />

        <provider
            android:name="org.citra.citra_emu.model.GameProvider"
            android:authorities="${applicationId}.provider"
            android:enabled="true"
            android:exported="false">
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.filesprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/nnf_provider_paths" />
        </provider>
    </application>

</manifest>
