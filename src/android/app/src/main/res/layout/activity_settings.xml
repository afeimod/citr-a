<?xml version="1.0" encoding="utf-8"?>
<!--
    修 11:原来只有一个空的 FrameLayout,ActionBar 是系统注入的。改用 NoActionBar 主题
    之后,这里要自己塞一个 LinearLayout (vertical),顶上是 Toolbar,下面是 fragment
    内容容器。Toolbar 的 fitsSystemWindows="true" 会让它自己让出状态栏高度,
    配合 SettingsActivity.setDecorFitsSystemWindows(false) 一起用,效果就是
    "toolbar 在状态栏下面,内容在 toolbar 下面"。

    注意:不要同时在 root 和 Toolbar 上设 fitsSystemWindows=true,会重复消费 insets,
    toolbar 顶上的 padding 会变成两倍。设一个就够。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_settings"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:fitsSystemWindows="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:elevation="4dp" />

    <FrameLayout
        android:id="@+id/frame_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
